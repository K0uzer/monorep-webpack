{
    "name": "monorep-webpack",
    "version": "1.0.0",
    "description": "monorep-webpack",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "workspaces": [
        "packages/*",
        "services/*"
    ]
}
